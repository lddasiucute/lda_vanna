% =====================================================================
%  BAO CAO KIEM THU HIEU NANG AI (LOAD TEST voi k6)
%  Form: Bao cao VNPT IT - Trung tam DMS
%  Bien dich bang: xelatex bao_cao_hieu_nang_ai.tex  (chay 2 lan de co muc luc)
% =====================================================================
\documentclass[12pt,a4paper]{article}

\usepackage{fontspec}
\IfFontExistsTF{Latin Modern Roman}
  {\setmainfont{Latin Modern Roman}}
  {\setmainfont{Times New Roman}}
\IfFontExistsTF{Latin Modern Sans}
  {\setsansfont{Latin Modern Sans}}
  {\setsansfont{Arial}}
\IfFontExistsTF{Latin Modern Mono}
  {\setmonofont{Latin Modern Mono}}
  {\setmonofont{Consolas}}
\usepackage{amsmath}
\usepackage{polyglossia}
\setdefaultlanguage{vietnamese}
\usepackage{geometry}
\geometry{top=2.25cm,bottom=2.2cm,left=2.35cm,right=2.15cm}
\usepackage{graphicx}
\usepackage[table]{xcolor}
\usepackage{array,booktabs,longtable,tabularx,multirow}
\usepackage{enumitem}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{hyperref}
\usepackage{caption}
\usepackage{float}
\usepackage{listings}
\usepackage{setspace}
\usepackage{microtype}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

\definecolor{vnptblue}{HTML}{006FD6}
\definecolor{deepblue}{HTML}{004E92}
\definecolor{lightblue}{HTML}{EEF6FF}
\definecolor{lightgray}{HTML}{F4F5F7}
\definecolor{darkgray}{HTML}{2B2B2B}
\definecolor{successgreen}{HTML}{15803D}
\definecolor{warnorange}{HTML}{C77700}
\definecolor{dangerred}{HTML}{C42828}

\newcommand{\ok}[1]{\textcolor{successgreen}{\bfseries #1}}
\newcommand{\warn}[1]{\textcolor{warnorange}{\bfseries #1}}
\newcommand{\bad}[1]{\textcolor{dangerred}{\bfseries #1}}

\hypersetup{
  colorlinks=true,
  linkcolor=vnptblue,
  urlcolor=deepblue,
  citecolor=deepblue,
  pdfauthor={Lê Đức Anh},
  pdftitle={Báo cáo Kiểm thử Hiệu năng Hệ thống Text-to-SQL},
  pdfsubject={Load test với k6, phân tích ngưỡng bão hoà và khuyến nghị}
}

\setlength{\parindent}{0.7cm}
\setlength{\parskip}{3pt}
\setlength{\headheight}{15pt}
\setlist[itemize]{leftmargin=1.1cm,itemsep=2pt,topsep=3pt}
\setlist[enumerate]{leftmargin=1.1cm,itemsep=3pt,topsep=3pt}
\renewcommand{\arraystretch}{1.2}

\titleformat{\section}
  {\Large\bfseries\color{vnptblue}}
  {\thesection.}{0.55em}{}
\titleformat{\subsection}
  {\large\bfseries\color{darkgray}}
  {\thesubsection.}{0.5em}{}
\titleformat{\subsubsection}
  {\normalsize\bfseries}
  {\thesubsubsection.}{0.5em}{}

\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\bfseries VNPT IT - TRUNG TÂM DMS}
\fancyhead[R]{\small\itshape Báo cáo Kiểm thử Hiệu năng Hệ thống AI}
\fancyfoot[L]{\small Người thực hiện: Lê Đức Anh}
\fancyfoot[R]{\small\thepage}
\renewcommand{\headrulewidth}{0.5pt}
\renewcommand{\footrulewidth}{0.5pt}

\newcommand{\methodbox}[2]{%
  \begin{center}
  \fcolorbox{vnptblue}{lightblue}{%
    \begin{minipage}{0.92\textwidth}
      \vspace{0.18cm}
      {\bfseries\color{deepblue}#1}\par
      \vspace{0.08cm}
      #2
      \vspace{0.18cm}
    \end{minipage}
  }
  \end{center}
}

\lstdefinelanguage{JS}{
  keywords={import,from,export,const,let,function,return,default,new,if,for},
  sensitive=true,
  morecomment=[l]{//},
  morestring=[b]',
  morestring=[b]"
}
\lstset{
  basicstyle=\ttfamily\small,
  backgroundcolor=\color{lightgray},
  frame=single,
  rulecolor=\color{gray},
  breaklines=true,
  showstringspaces=false,
  columns=fullflexible
}

\begin{document}

% ======================= TRANG BIA =======================
\begin{titlepage}
\thispagestyle{empty}

\begin{minipage}[t]{0.24\textwidth}
  \vspace{0pt}
  \IfFileExists{vnpt_logo.png}{
    \includegraphics[width=3.25cm]{vnpt_logo.png}
  }{
    \fcolorbox{vnptblue}{white}{
      \parbox[c][1.5cm][c]{3.0cm}{
        \centering\color{vnptblue}\Large\bfseries VNPT
      }
    }
  }
\end{minipage}
\hfill
\begin{minipage}[t]{0.70\textwidth}
  \vspace{0.8cm}
  \begin{center}
  {\bfseries TẬP ĐOÀN BƯU CHÍNH VIỄN THÔNG VIỆT NAM}\\[2pt]
  {\bfseries VNPT IT - TRUNG TÂM DMS}\\[3pt]
  {\itshape Bộ phận Phát triển Giải pháp AI}
  \end{center}
\end{minipage}

\vspace{0.55cm}

\begin{center}
{\LARGE\bfseries BÁO CÁO KIỂM THỬ HIỆU NĂNG}\\[0.25cm]
{\LARGE\bfseries HỆ THỐNG HỎI ĐÁP DỮ LIỆU TEXT-TO-SQL}\\[0.12cm]
{\LARGE\bfseries DƯỚI TẢI NGƯỜI DÙNG ĐỒNG THỜI}\\[0.30cm]

{\large\bfseries Load Test bằng công cụ k6 --- Xác định ngưỡng bão hoà}\\
{\large\bfseries trên nền tảng Vanna, Ollama \texttt{llama3.2} và PostgreSQL Neon}\\[0.42cm]

\rule{0.47\textwidth}{1.5pt}
\end{center}

\vspace{0.42cm}

\begin{center}
\setlength{\fboxsep}{0pt}
\fcolorbox{vnptblue}{white}{%
\begin{minipage}{0.94\textwidth}
\colorbox{vnptblue}{\parbox{\linewidth}{%
  \centering\color{white}\bfseries\large
  PHẠM VI KIỂM THỬ}}
\vspace{0.15cm}
\begin{minipage}{0.92\linewidth}
\hspace{0.45cm}
\small
Báo cáo trình bày kết quả kiểm thử tải (load test) endpoint hỏi đáp AI
\texttt{/api/vanna/v2/chat\_sse} của hệ thống PoC Text-to-SQL. Mục tiêu là
mô phỏng số lượng người dùng đồng thời tăng dần (5, 15, 30 và 50 người dùng),
đo thời gian phản hồi, thông lượng và tỉ lệ lỗi, từ đó xác định điểm bão hoà
và đề xuất phương án nâng cao khả năng chịu tải.
\end{minipage}
\vspace{0.15cm}
\end{minipage}}
\end{center}

\vspace{0.18cm}

\begin{center}
\setlength{\fboxsep}{0pt}
\fcolorbox{darkgray}{white}{
\begin{minipage}{0.94\textwidth}
\colorbox{darkgray}{\parbox{\linewidth}{%
  \centering\color{white}\bfseries Tóm tắt thông số kiểm thử}}
\vspace{0.10cm}
\small
\begin{tabularx}{0.93\textwidth}{>{\bfseries}p{4.2cm}X}
Công cụ kiểm thử: & k6 v2.1.0, kịch bản \texttt{ramping-vus}\\
Đối tượng: & Endpoint SSE của Vanna 2.0 (FastAPI, Uvicorn 1 worker)\\
Hạ tầng AI: & Ollama \texttt{llama3.2} cục bộ, PostgreSQL trên Neon\\
Mức tải: & 5 $\to$ 15 $\to$ 30 người dùng và cao điểm 50 người dùng\\
Kết quả chính: & Ngưỡng bão hoà $\approx$ 5--10 người dùng đồng thời\\
\end{tabularx}
\vspace{0.10cm}
\end{minipage}}
\end{center}

\vfill

\begin{flushright}\small
\textbf{Người thực hiện:}\\
\textit{Lê Đức Anh}\\
Trung tâm DMS\\[0.28cm]
\textbf{Cấp báo cáo:}\\
\textit{Ban Lãnh đạo Trung tâm DMS}
\end{flushright}

\end{titlepage}

\thispagestyle{empty}
\vspace*{\fill}
\begin{center}
{\large Hà Nội, Tháng 08/2026}
\end{center}
\vspace*{\fill}
\clearpage

\setcounter{page}{1}
\setcounter{tocdepth}{1}
\tableofcontents
\clearpage

% ======================= 1. EXECUTIVE SUMMARY =======================
\section{Tóm tắt kết quả cho Lãnh đạo (Executive Summary)}

Báo cáo đánh giá khả năng chịu tải của hệ thống hỏi đáp dữ liệu bằng ngôn ngữ
tự nhiên (Text-to-SQL) khi số lượng người dùng truy cập đồng thời tăng cao.
Công cụ \textbf{k6} được dùng để phát sinh tải thực tế lên endpoint streaming
\texttt{/api/vanna/v2/chat\_sse}, mô phỏng các câu hỏi nghiệp vụ tiếng Việt.

Kết quả cho thấy hệ thống PoC vận hành \textbf{ổn định dưới 10 người dùng đồng
thời}: thời gian phản hồi nhanh, gần như không có lỗi. Tuy nhiên, khi vượt
ngưỡng này, hệ thống \textbf{bão hoà}: thông lượng không tăng thêm mà độ trễ và
tỉ lệ lỗi tăng phi tuyến. Ở mức 50 người dùng, độ trễ p95 đạt \bad{139 giây} và
tỉ lệ lỗi lên tới \bad{13\%}, không đạt trải nghiệm chấp nhận được.

Nguyên nhân cốt lõi là mô hình ngôn ngữ \texttt{llama3.2} chạy cục bộ xử lý gần
như tuần tự, trong khi mỗi câu hỏi cần hai lần gọi mô hình. Đây là nút thắt cổ
chai quyết định năng lực của toàn hệ thống \cite{ollama2026,vanna2026}.

\methodbox{Kết luận nhanh}{%
Điểm bão hoà của cấu hình hiện tại là $\approx$ 5--10 người dùng đồng thời, với
thông lượng trần khoảng 0,7--0,75 request/giây. Muốn phục vụ số lượng người dùng
lớn hơn, bắt buộc phải gỡ nút thắt mô hình ngôn ngữ (tăng song song Ollama hoặc
chuyển sang API LLM đám mây) và chạy máy chủ ứng dụng đa tiến trình.}

% ======================= 2. MOI TRUONG =======================
\section{Môi trường và phương pháp kiểm thử}

\subsection{Cấu hình môi trường}

\begin{table}[H]
\centering
\caption{Cấu hình môi trường kiểm thử}
\begin{tabularx}{\textwidth}{>{\bfseries}p{4.2cm}X}
\toprule
Hạng mục & Chi tiết\\
\midrule
Công cụ kiểm thử & k6 v2.1.0 (Windows/amd64)\\
Endpoint & \texttt{POST /api/vanna/v2/chat\_sse} (Server-Sent Events)\\
Mô hình AI & Ollama \texttt{llama3.2} (chạy cục bộ, không tốn phí API)\\
Cơ sở dữ liệu & PostgreSQL --- Neon, schema \texttt{qlsp\_backup}\\
Máy chủ ứng dụng & Uvicorn (1 worker), FastAPI, Vanna 2.0\\
Loại truy vấn & Câu hỏi tiếng Việt (đếm và tổng hợp đơn giản)\\
Tiêu chí thành công & HTTP 200 và stream kết thúc bằng \texttt{[DONE]}\\
Timeout mỗi request & 180 giây\\
Baseline (1 người dùng) & $\approx 2{,}5$ giây/truy vấn\\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Cấu hình phần cứng}

Toàn bộ hệ thống (ứng dụng, mô hình Ollama và công cụ k6) chạy trên cùng một máy
trạm; cơ sở dữ liệu PostgreSQL đặt từ xa trên Neon (cloud). Đây là mô hình
kiểm thử một máy (single-node), phù hợp cho giai đoạn PoC.

\begin{table}[H]
\centering
\caption{Thông số phần cứng máy chạy kiểm thử}
\begin{tabularx}{\textwidth}{>{\bfseries}p{4.2cm}X}
\toprule
Thành phần & Thông số\\
\midrule
CPU & AMD Ryzen 7 8845H (8 nhân / 16 luồng)\\
GPU & AMD Radeon 780M (đồ hoạ tích hợp)\\
RAM & 16 GB (khoảng 13,8 GB khả dụng; phần còn lại chia sẻ cho GPU tích hợp)\\
Hệ điều hành & Windows 11 Home\\
Vị trí cơ sở dữ liệu & Neon (PostgreSQL đám mây, kết nối qua SSL)\\
\bottomrule
\end{tabularx}
\end{table}

\noindent\textbf{Ghi chú:} Do máy sử dụng \textbf{GPU tích hợp} (không có GPU rời),
mô hình \texttt{llama3.2} suy luận chủ yếu trên CPU. Đây là yếu tố phần cứng trực
tiếp giới hạn tốc độ và mức độ song song của mô hình, và cần được tính đến khi
diễn giải kết quả kiểm thử.

\subsection{Đặc điểm kỹ thuật ảnh hưởng đến hiệu năng}

\begin{itemize}
  \item Mỗi câu hỏi kích hoạt \textbf{hai lần gọi mô hình ngôn ngữ}: một lần để
  sinh câu lệnh SQL, một lần để diễn giải kết quả trả về cho người dùng.
  \item Mỗi request sử dụng một \texttt{conversation\_id} riêng biệt để tránh cơ
  chế khoá theo hội thoại (HTTP 409) của máy chủ, bảo đảm tải thực sự song song.
  \item Ollama cục bộ xử lý các yêu cầu suy luận gần như \textbf{tuần tự}, nên là
  điểm giới hạn thông lượng chính của hệ thống.
\end{itemize}

\subsection{Kịch bản kiểm thử}

Hai kịch bản tăng tải (\texttt{ramping-vus}) được thực hiện:

\begin{enumerate}
  \item \textbf{Kịch bản A --- Tăng dần:} $5 \to 15 \to 30$ người dùng đồng thời,
  tổng thời gian $\approx 3$ phút 35 giây. Mục tiêu quan sát diễn biến khi tải
  tăng từ thấp đến trung bình.
  \item \textbf{Kịch bản B --- Cao điểm 50:} tăng nhanh lên $50$ người dùng và giữ
  trong 120 giây. Mục tiêu kiểm tra hành vi khi vượt xa ngưỡng bão hoà.
\end{enumerate}

\begin{lstlisting}[language=JS,caption={Trích cấu hình kịch bản tăng tải trong k6}]
export const options = {
  scenarios: {
    ramping_users: {
      executor: 'ramping-vus',
      stages: [
        { duration: '20s', target: 5  },  // 5 nguoi dung
        { duration: '20s', target: 15 },  // tang len 15
        { duration: '60s', target: 30 },  // cao diem 30
      ],
    },
  },
};
\end{lstlisting}

\subsection{Bộ câu hỏi kiểm thử}

Mỗi người dùng ảo chọn ngẫu nhiên một câu hỏi trong tập câu hỏi nghiệp vụ tiếng
Việt dưới đây. Các câu hỏi thuộc nhóm đếm và tổng hợp đơn giản nhằm cô lập chi phí
suy luận của mô hình (tách khỏi chi phí truy vấn SQL phức tạp):

\begin{enumerate}
  \item ``Có bao nhiêu người dùng trong hệ thống?''
  \item ``Có bao nhiêu đơn vị trong bảng \texttt{units}?''
  \item ``Có bao nhiêu \texttt{spdv\_tickets}?''
  \item ``Đếm số người dùng theo trạng thái.''
  \item ``Liệt kê 5 đơn vị đầu tiên theo tên.''
  \item ``Có bao nhiêu sự kiện trong \texttt{spdv\_ticket\_events}?''
\end{enumerate}

% ======================= 3. KET QUA =======================
\section{Kết quả kiểm thử}

\subsection{Kết quả tổng quan}

\begin{table}[H]
\centering
\caption{So sánh kết quả tổng quan theo mức tải}
\begin{tabularx}{\textwidth}{>{\bfseries}Xccc}
\toprule
\rowcolor{lightblue}
Chỉ số & 1 user (baseline) & 30 user (A) & 50 user (B)\\
\midrule
Tổng số request        & 1     & 177 & 132\\
Thông lượng (req/giây)  & 0,40 & 0,75 & 0,70\\
Tỉ lệ thành công        & 100\% & \ok{95,0\%} & \warn{87,0\%}\\
Số lỗi (rớt kết nối EOF) & 0    & 9 & 17\\
Tỉ lệ lỗi HTTP          & 0\%   & \warn{5,0\%} & \bad{13,0\%}\\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Phân bố thời gian phản hồi AI}

\begin{table}[H]
\centering
\caption{Phân bố thời gian phản hồi AI theo mức tải (đơn vị: giây)}
\begin{tabularx}{\textwidth}{>{\bfseries}Xccc}
\toprule
\rowcolor{lightblue}
Thời gian phản hồi & Baseline & 30 user & 50 user\\
\midrule
Nhanh nhất (min)   & --    & 0,59  & 1,34\\
Trung vị (median)  & 2,5   & 19,43 & 23,26\\
Trung bình (avg)   & 2,5   & 20,90 & \bad{36,28}\\
p90                & --    & 32,71 & \bad{108,40}\\
p95                & --    & 55,11 & \bad{139,16}\\
Chậm nhất (max)    & 2,5   & 89,28 & \bad{157,20}\\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Biểu đồ trực quan}

Hình~\ref{fig:latency} so sánh phân bố thời gian phản hồi giữa hai mức tải. Có thể
thấy độ trễ tăng rất mạnh ở các percentile cao khi chuyển từ 30 lên 50 người dùng:
p95 nhảy từ 55 giây lên 139 giây.

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
  ybar, bar width=13pt,
  width=0.95\textwidth, height=7cm,
  ylabel={Thời gian phản hồi (giây)},
  symbolic x coords={Median, p90, p95, Max},
  xtick=data,
  ymin=0, ymax=175,
  legend style={at={(0.02,0.98)},anchor=north west,font=\small},
  nodes near coords, nodes near coords style={font=\tiny,/pgf/number format/fixed},
  enlarge x limits=0.22,
]
\addplot[fill=vnptblue,draw=deepblue] coordinates {(Median,19.43) (p90,32.71) (p95,55.11) (Max,89.28)};
\addplot[fill=warnorange!80,draw=dangerred] coordinates {(Median,23.26) (p90,108.40) (p95,139.16) (Max,157.20)};
\legend{30 người dùng, 50 người dùng}
\end{axis}
\end{tikzpicture}
\caption{So sánh phân bố thời gian phản hồi AI ở mức 30 và 50 người dùng đồng thời.}
\label{fig:latency}
\end{figure}

Hình~\ref{fig:scaling} cho thấy đặc trưng bão hoà: thời gian phản hồi trung bình
tăng gần tuyến tính theo số người dùng, trong khi thông lượng không tăng --- dấu
hiệu điển hình của một hệ thống đã chạm trần xử lý.

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
  width=0.95\textwidth, height=6.5cm,
  xlabel={Số người dùng đồng thời}, ylabel={Thời gian TB (giây)},
  xtick={1,30,50}, ymin=0,
  ymajorgrids=true, grid style={dashed,gray!30},
  mark size=2.6pt,
  nodes near coords, nodes near coords style={font=\small},
  point meta=explicit symbolic,
]
\addplot[color=vnptblue,thick,mark=*] coordinates {
  (1,2.5)[2,5s] (30,20.9)[20,9s] (50,36.28)[36,3s]
};
\end{axis}
\end{tikzpicture}
\caption{Thời gian phản hồi trung bình tăng gần tuyến tính theo số người dùng
(baseline $\to$ 30 $\to$ 50), trong khi thông lượng giữ nguyên $\approx 0{,}7$ req/giây.}
\label{fig:scaling}
\end{figure}

\subsection{Diễn biến theo mức tải}

\begin{table}[H]
\centering
\caption{Hành vi hệ thống quan sát theo từng giai đoạn tải}
\begin{tabularx}{\textwidth}{>{\bfseries}p{2.6cm}cX}
\toprule
Giai đoạn & Số user & Hành vi hệ thống\\
\midrule
Tải thấp   & 5  & Ổn định, phản hồi nhanh, không có lỗi\\
Tải trung  & 15 & Độ trễ tăng rõ, xuất hiện lỗi rớt kết nối đầu tiên\\
Tải cao    & 30 & Thông lượng chững lại, nhiều lỗi EOF, tỉ lệ lỗi 5\%\\
Quá tải    & 50 & Bão hoà hoàn toàn, p95 tới 139 giây, tỉ lệ lỗi 13\%\\
\bottomrule
\end{tabularx}
\end{table}

% ======================= 4. PHAN TICH =======================
\section{Phân tích kết quả}

\subsection{Thông lượng đạt trần}

Thông lượng gần như không đổi giữa 30 và 50 người dùng ($0{,}75$ so với $0{,}70$
request/giây). Điều này chứng minh hệ thống đã \textbf{đạt trần xử lý} ở mức
$\approx 0{,}7$--$0{,}75$ request/giây. Việc tăng thêm người dùng không làm tăng
năng lực xử lý mà chỉ kéo dài hàng đợi, khiến độ trễ và tỉ lệ lỗi tăng vọt.

\subsection{Giải thích bằng định luật Little}

Định luật Little trong lý thuyết hàng đợi phát biểu:
\[
 L = \lambda \times W
\]
trong đó $L$ là số yêu cầu trung bình đang trong hệ thống (mức đồng thời),
$\lambda$ là thông lượng (request/giây) và $W$ là thời gian phản hồi trung bình.
Khi hệ thống đã bão hoà, thông lượng $\lambda$ bị chặn trên bởi năng lực xử lý:
\[
 \lambda_{\max} \approx \frac{N_{\text{song song}}}{W_{\text{phục vụ}}}
\]
Với thời gian phục vụ cơ sở $W_{\text{phục vụ}} \approx 2{,}5$ giây (đo ở baseline)
và thông lượng trần quan sát được $\approx 0{,}75$ req/giây, có thể suy ra mức song
song hiệu dụng $N_{\text{song song}} \approx 0{,}75 \times 2{,}5 \approx 1{,}9$.
Con số này phù hợp với việc Ollama chỉ xử lý được khoảng \textbf{2 yêu cầu song
song} trên cấu hình phần cứng hiện tại (CPU + GPU tích hợp). Khi số người dùng
vượt xa mức này, mọi yêu cầu tăng thêm chỉ làm dài thời gian chờ $W$ (theo đúng
$L = \lambda \times W$ với $\lambda$ cố định), chứ không nâng được thông lượng.

\subsection{Nút thắt cổ chai: mô hình ngôn ngữ cục bộ}

Nguyên nhân chính là \textbf{Ollama chạy cục bộ xử lý gần như tuần tự}. Với hai
lần gọi mô hình cho mỗi câu hỏi, khi có 30--50 người dùng cùng lúc, các yêu cầu
xếp hàng dài. Thời gian phản hồi tăng từ $2{,}5$ giây (1 người dùng) lên trung vị
$\approx 23$ giây và p95 tới $139$ giây (50 người dùng) --- tức chậm gấp
\textbf{hơn 50 lần} so với baseline.

\subsection{Lỗi rớt kết nối}

Toàn bộ lỗi thuộc dạng \texttt{EOF}: máy chủ chủ động đóng kết nối khi quá tải.
Tỉ lệ lỗi tăng từ $5\%$ (30 người dùng) lên $13\%$ (50 người dùng). Nguyên nhân
là sự kết hợp giữa hàng đợi Ollama quá dài, giới hạn số kết nối tới Neon, và
Uvicorn chỉ chạy \textbf{một tiến trình worker} duy nhất.

\methodbox{Nhận định trọng yếu}{%
Năng lực của hệ thống được quyết định bởi tốc độ suy luận của mô hình ngôn ngữ,
không phải bởi cơ sở dữ liệu hay tầng ứng dụng. Do đó, mọi phương án nâng cao khả
năng chịu tải phải ưu tiên gỡ nút thắt LLM trước tiên.}

% ======================= TIEU CHI NGHIEM THU =======================
\section{Đánh giá theo tiêu chí nghiệm thu (SLA)}

Bảng~\ref{tab:sla} đối chiếu kết quả đo với các ngưỡng chất lượng dịch vụ đề xuất
cho một hệ thống hỏi đáp tương tác. Ở mức 30 và 50 người dùng, hệ thống
\textbf{không đạt} cả ba tiêu chí; các ngưỡng này chỉ được thoả mãn khi số người
dùng đồng thời ở mức thấp ($\leq 10$).

\begin{table}[H]
\centering
\caption{Đối chiếu kết quả với tiêu chí nghiệm thu đề xuất}
\label{tab:sla}
\begin{tabularx}{\textwidth}{>{\bfseries}Xccc}
\toprule
\rowcolor{lightblue}
Tiêu chí & Ngưỡng mục tiêu & 30 user & 50 user\\
\midrule
Độ trễ p95        & $< 10$ giây & \bad{Không đạt} & \bad{Không đạt}\\
Tỉ lệ thành công  & $\geq 99\%$ & \bad{Không đạt} & \bad{Không đạt}\\
Tỉ lệ lỗi         & $\leq 1\%$  & \bad{Không đạt} & \bad{Không đạt}\\
\bottomrule
\end{tabularx}
\end{table}

\methodbox{Sức chứa an toàn ước tính}{%
Với ngưỡng p95 dưới 10 giây và tỉ lệ lỗi dưới 1\%, cấu hình hiện tại chỉ nên phục
vụ tối đa \textbf{5--10 người dùng đồng thời}. Để phục vụ nhiều hơn, cần áp dụng
các khuyến nghị ở Mục~7 trước khi đưa vào sử dụng thực tế.}

% ======================= GIOI HAN =======================
\section{Giới hạn của kiểm thử}

Kết quả trong báo cáo cần được diễn giải trong phạm vi các giới hạn sau:

\begin{itemize}
  \item \textbf{Mô hình một máy:} ứng dụng, mô hình Ollama và công cụ k6 chạy trên
  cùng một máy, nên có sự cạnh tranh tài nguyên CPU giữa bên tạo tải và bên xử lý.
  \item \textbf{Phần cứng tiêu dùng:} máy dùng GPU tích hợp, không đại diện cho cấu
  hình máy chủ production có GPU rời; kết quả tuyệt đối sẽ khác trên hạ tầng mạnh hơn.
  \item \textbf{Câu hỏi đơn giản:} bộ câu hỏi thiên về đếm/tổng hợp; câu hỏi phức
  tạp (nhiều join, cửa sổ thời gian) sẽ làm tăng cả chi phí SQL lẫn suy luận.
  \item \textbf{Độ dài kiểm thử:} mỗi kịch bản kéo dài vài phút, chưa đánh giá độ ổn
  định dài hạn (rò rỉ bộ nhớ, suy giảm hiệu năng theo thời gian).
  \item \textbf{Cơ sở dữ liệu đám mây:} độ trễ mạng tới Neon có thể dao động theo
  thời điểm, ảnh hưởng nhỏ đến kết quả.
\end{itemize}

% ======================= 5. KET LUAN =======================
\section{Kết luận}

\begin{center}
\setlength{\fboxsep}{6pt}
\fcolorbox{vnptblue}{lightblue}{\parbox{0.92\textwidth}{\centering
\textbf{Điểm bão hoà của hệ thống hiện tại $\approx$ 5--10 người dùng đồng thời.}\\[3pt]
Dưới 10 người dùng: phản hồi nhanh, gần như không lỗi. Từ 15 người dùng: độ trễ
tăng phi tuyến, bắt đầu rớt kết nối. Ở 30--50 người dùng: p95 đạt 55--139 giây và
5--13\% request thất bại --- không đạt trải nghiệm chấp nhận được cho người dùng
thực.}}
\end{center}

Kết quả khẳng định rằng để phục vụ số lượng người dùng lớn hơn, việc tối ưu tầng
ứng dụng là chưa đủ; cần đầu tư vào năng lực suy luận của mô hình ngôn ngữ và kiến
trúc phục vụ đồng thời.

% ======================= 6. KHUYEN NGHI =======================
\section{Khuyến nghị cải thiện}

\begin{longtable}{>{\bfseries}p{1.9cm}p{8.6cm}p{3.6cm}}
\caption{Các khuyến nghị nâng cao khả năng chịu tải}\\
\toprule
Ưu tiên & Giải pháp & Kỳ vọng\\
\midrule
\endfirsthead
\toprule
Ưu tiên & Giải pháp & Kỳ vọng\\
\midrule
\endhead
\textcolor{dangerred}{Cao} & Chạy Uvicorn nhiều worker (\texttt{-{}-workers 4}) hoặc đặt sau Gunicorn &
Giảm rớt kết nối, tận dụng đa nhân\\
\textcolor{dangerred}{Cao} & Tăng song song Ollama (\texttt{OLLAMA\_NUM\_PARALLEL}) hoặc chuyển sang API đám mây (Anthropic/OpenAI) cho tải cao &
Gỡ nút thắt LLM --- yếu tố quyết định\\
\textcolor{warnorange}{Trung bình} & Cache câu hỏi và câu lệnh SQL lặp lại &
Giảm số lần gọi mô hình\\
\textcolor{warnorange}{Trung bình} & Cấu hình connection pool tới Neon hợp lý, tăng timeout &
Giảm lỗi EOF\\
\textcolor{successgreen}{Thấp} & Thêm hàng đợi và thông báo ``đang xử lý'' cho người dùng &
Cải thiện trải nghiệm khi tải cao\\
\bottomrule
\end{longtable}

\vfill
\begin{flushright}
\textbf{Người thực hiện}\\
\textit{(Đã lập báo cáo)}\\[0.8cm]
\textbf{Lê Đức Anh}
\end{flushright}

\clearpage
% ======================= PHU LUC =======================
\section*{Phụ lục A. Kịch bản k6 (rút gọn)}
\addcontentsline{toc}{section}{Phụ lục A. Kịch bản k6}

\begin{lstlisting}[language=JS,caption={Logic phát tải và đo lường của mỗi người dùng ảo}]
import http from 'k6/http';
import { check } from 'k6';
import { Trend, Rate, Counter } from 'k6/metrics';

const aiResponseTime = new Trend('ai_response_time', true);
const aiSuccessRate  = new Rate('ai_success_rate');
const aiErrors       = new Counter('ai_errors');

export default function () {
  // conversation_id duy nhat -> tranh khoa 409 theo hoi thoai
  const uid = `lt-${__VU}-${__ITER}-${Date.now()}-${Math.random()}`;
  const question = QUESTIONS[Math.floor(Math.random() * QUESTIONS.length)];

  const res = http.post(ENDPOINT,
    JSON.stringify({ message: question, conversation_id: uid, request_id: uid }),
    { headers: { 'Content-Type': 'application/json' }, timeout: '180s' });

  aiResponseTime.add(res.timings.duration);
  const ok = res.status === 200 && String(res.body).includes('data: [DONE]');
  aiSuccessRate.add(ok);
  if (!ok) aiErrors.add(1);

  check(res, {
    'HTTP 200': (r) => r.status === 200,
    'stream hoan tat': (r) => String(r.body).includes('data: [DONE]'),
  });
}
\end{lstlisting}

\noindent Mã nguồn đầy đủ và số liệu thô có trong thư mục \texttt{loadtest/} của
kho mã: \texttt{ai\_load\_test.js}, \texttt{ai\_load\_test\_50.js},
\texttt{k6\_summary.json}, \texttt{k6\_summary\_50.json}.

\clearpage
\renewcommand{\refname}{Tài liệu tham khảo}
\addcontentsline{toc}{section}{Tài liệu tham khảo}
\begin{thebibliography}{9}

\bibitem{k62026}
Grafana Labs,
\textit{k6 --- Load testing tool for engineering teams},
tài liệu công cụ kiểm thử tải mã nguồn mở, phiên bản 2.1.0.
\url{https://k6.io/docs/}.
Truy cập ngày 04/08/2026.

\bibitem{vanna2026}
Vanna AI,
\textit{Vanna 2.0: Turn Questions into Data Insights},
GitHub repository \texttt{vanna-ai/vanna}, phiên bản mã nguồn tham chiếu 2.0.2.
\url{https://github.com/vanna-ai/vanna}.
Truy cập ngày 04/08/2026.

\bibitem{ollama2026}
Ollama,
\textit{Ollama --- Run large language models locally},
tài liệu cấu hình song song (\texttt{OLLAMA\_NUM\_PARALLEL}) và phục vụ mô hình.
\url{https://github.com/ollama/ollama}.
Truy cập ngày 04/08/2026.

\bibitem{neon2026}
Neon,
\textit{Connection pooling},
tài liệu kết nối PostgreSQL và pooled connection cho ứng dụng.
\url{https://neon.com/docs/connect/connection-pooling}.
Truy cập ngày 04/08/2026.

\end{thebibliography}

\end{document}
